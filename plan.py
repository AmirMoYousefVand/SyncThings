import customtkinter as ctk

# 1. Best way to toggle between 'Dark' and 'Light' in customtkinter
# 2. change some specific colors dynamically.
# 3. Best way to implement a language dictionary that we can switch at runtime (e.g., using a global dict, or variables). Note that the codebase already has a format_persian function for RTL.
